import React from 'react';
import ProfileScreen from '../profile/ProfileScreen';

export const SettingsScreen: React.FC<any> = (props) => {
  return <ProfileScreen {...props} />;
};

export default SettingsScreen;
